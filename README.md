# README

These are all the game files used to build the game "Gravitate".
If you want to put this into the unity editor, replace the "Assets", "Packages", and "ProjectSettings" folders with these ones.
